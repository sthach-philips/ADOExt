# Changesets

This folder is managed by `@changesets/cli`. It holds changeset files that
describe pending version bumps and changelog entries.

Add a changeset when you make a user-visible change:

```bash
pnpm changeset
```

To consume changesets and bump the version:

```bash
pnpm changeset version
```
