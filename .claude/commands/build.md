Run `pnpm run compile` to build the ADOExt extension (TypeScript type check + esbuild bundle).

Parse the output for errors and warnings. Report each one with its exact file path and line number in the format `path/to/file.ts:line — message`. If there are no errors, confirm the build succeeded and state the output files produced.
