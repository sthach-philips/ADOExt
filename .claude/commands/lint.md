Run ESLint across all TypeScript source files.

Execute: `pnpm run lint`

This project enforces `--max-warnings=0`, so any warning is a failure. Report each issue with file:line, the rule name, and the message. If clean, confirm lint passed with zero warnings.
